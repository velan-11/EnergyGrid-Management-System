spring.application.name=identity-service
spring.config.import=optional:configserver:http://localhost:8888

# The mail health indicator opens an SMTP connection to smtp.gmail.com on every
# /actuator/health call. When outbound SMTP is blocked the probe hangs, so the
# aggregate health endpoint stalls. Mail send still works; only the probe is off.
management.health.mail.enabled=false